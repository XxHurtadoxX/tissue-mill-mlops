-r requirements-dev.txt

# Seguimiento de experimentos y guardado de modelos. Va aparte de las
# dependencias de desarrollo por una razón práctica: MLflow arrastra una
# veintena de paquetes, entre ellos Flask, gunicorn, docker y matplotlib, y
# dominaba el tiempo de instalación de la integración continua.
#
# Las pruebas no lo necesitan. Solo usan constantes y lógica pura de los módulos
# que lo importan, así que esas importaciones se hacen dentro de las funciones
# que de verdad lo utilizan y no al cargar el módulo.
#
# La versión está fijada por el conector de Azure, que exige mlflow-skinny
# hasta la 3.13.0. Subirla rompe la instalación.
mlflow==3.13.0

# Conector para registrar experimentos desde esta máquina hacia el workspace de
# Azure Machine Learning, que actúa como servidor de seguimiento de MLflow.
azureml-mlflow>=1.62.0

-r requirements.txt

# Herramientas de calidad que corre la integración continua en cada cambio.
flake8==7.1.1
pytest==8.3.4
# Para validar las definiciones YAML de Azure.
pyyaml==6.0.2

# Modelado. Las pruebas importan estos módulos, así que la integración continua
# los necesita.
scikit-learn==1.6.1
# La versión está fijada por compatibilidad con azureml-mlflow, que exige
# mlflow-skinny<=3.13.0. Subir mlflow aquí rompe la instalación del entorno de
# trabajo local, que sí instala ese conector.
mlflow==3.13.0

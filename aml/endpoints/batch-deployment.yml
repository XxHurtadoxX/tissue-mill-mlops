$schema: https://azuremlschemas.azureedge.net/latest/batchDeployment.schema.json

name: produccion
endpoint_name: tissue-falla-14d-lotes
description: >
  Versión del modelo que atiende hoy. Bajo un mismo endpoint pueden convivir
  varios despliegues con uno marcado como predeterminado: liberar una versión
  nueva es cambiar cuál lo es, y volver atrás es cambiarlo de vuelta, sin
  redesplegar nada.

model: azureml:tissue-falla-14d:1

# Entorno y script propios, en vez del despliegue sin código que el formato
# MLflow permitiría.
#
# Se intentó primero esa vía y falló al construir la imagen: Azure genera el
# entorno a partir del modelo y le añade paquetes propios que arrastran una
# versión antigua de matplotlib, que ya no compila en Python moderno porque usa
# una función del módulo platform eliminada hace varias versiones. El problema
# no está en el modelo sino en lo que la generación automática añade encima.
#
# Declarar el entorno evita ese añadido, y a cambio obliga a escribir el script
# de puntuación.
environment: azureml:tissue-training@latest
code_configuration:
  code: ../..
  scoring_script: src/model/batch_score.py

# Aquí está el ahorro. Se usa el clúster que ya existe, y como tiene mínimo cero
# nodos, al terminar el trabajo se apaga solo. Un endpoint en línea, en cambio,
# mantiene instancias dedicadas encendidas esperando peticiones que llegan una
# vez al día.
compute: azureml:cpu-cluster

resources:
  instance_count: 1

# Con doce equipos el tamaño del lote es indiferente, pero el parámetro es el
# que permite escalar: con miles de filas se reparten entre los nodos.
mini_batch_size: 10
max_concurrency_per_instance: 1
retry_settings:
  max_retries: 2
  timeout: 300

output_action: append_row
output_file_name: predicciones.csv
error_threshold: -1
logging_level: info

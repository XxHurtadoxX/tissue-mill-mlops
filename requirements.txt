# Dependencias de la construcción del dataset y, más adelante, del entrenamiento.
# El simulador (src/simulator) sigue funcionando solo con la librería estándar,
# así que el workflow que genera datos a diario no necesita instalar nada.
pandas==2.2.3
numpy==2.1.3
